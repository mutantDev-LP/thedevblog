{{#each errors}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}
{{/each}}

<h3>EDIT POST:</h3>

<div class="card">
    <div class="card-body">
        <form action="/admin/post/edit" method="POST" class="formdiv">
            <input type="hidden" value="{{post._id}}" name="id"/>
            <label for="title" style="text-tranform: uppercase; font-weight: bold;">Title: </label>
            <input type="text" id="title" name="title" placeholder="Post Title" value="{{post.title}}" required style="text-tranform: uppercase; font-weight: bold;" class="form-control">

            <label for="slug" style="text-tranform: uppercase; font-weight: bold;">Slug: </label>
            <input type="text" id="slug" name="slug" placeholder="Tag Slug" value="{{post.slug}}" class="form-control" required style="text-tranform: uppercase; font-weight: bold;">
            
            <label for="desc" style="text-tranform: uppercase; font-weight: bold;">Description: </label>
            <input type="text" id="desc" name="desc" placeholder="Post Description" value="{{post.desc}}" class="form-control" required style="text-tranform: uppercase; font-weight: bold;">

            <label for="content" style="text-tranform: uppercase; font-weight: bold;">Post: </label>
            <textarea name="content" required  style="text-tranform: uppercase; font-weight: bold;" class="form-control" >{{post.content}}</textarea>

            <label for="tag" style="text-tranform: uppercase; font-weight: bold;">Tag: </label>
            <select name="tag" class="form-select" place-holder="Select Tag">
                {{#each tags}}
                    <option value="{{_id}}">
                        {{name}}
                    </option>
                {{else}}
                    <option value="0">
                        Nenhuma Categoria Registrada!
                    </option>
                {{/each}}
            </select>

            <br>

            <button type="submit" class="btn" style="color: white; background-color: rgb(100, 0, 0);
            box-shadow: 2px 2px rgb(63, 0, 0); position: relative; text-transform: uppercase; font-weight: bold;">Edit Post</button>
        </form>
    </div>
</div>


<style>
    .formdiv {
        padding: 10px
    }

    .card {
        width: 60%;
        background-color: rgb(4, 4, 24);
        border: 0px;
        box-shadow: 4px 4px rgb(63, 0, 0), 8px 8px rgb(4, 4, 24);
    }

    .btn:hover {
        background-color: rgb(63, 0, 0);
        box-shadow: 2px 2px rgb(27, 0, 0);
    }

    .btn:active {
        top: 2px;
        left: 2px;
        background-color: rgb(27, 0, 0);
    }
</style>